import { typeDefs } from './typeDefs'; // Import your GraphQL type definitions
import { resolvers } from './resolvers'; // Import your GraphQL resolvers

export { typeDefs, resolvers }; // Export your type definitions and resolvers